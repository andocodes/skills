{
  "name": "@cursor-skill/swarm",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "Local worktree orchestration runtime for the /swarm skill.",
  "scripts": {
    "cli": "bun cli.ts",
    "test": "bun test",
    "typecheck": "tsc --noEmit",
    "check": "tsc --noEmit && bun test"
  },
  "dependencies": {
    "@cursor/sdk": "^1.0.8",
    "commander": "^12.0.0",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "latest"
  }
}
